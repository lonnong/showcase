- @posts.each do |post|
	= link_to (image_tag post.image(:small))
	%h2= link_to post.title, post
	%p
		= post.comments.count
		Comments
		%p
			= post.get_likes.size
			Likes
%h2 Add a post
%button= link_to "New Post", new_post_path