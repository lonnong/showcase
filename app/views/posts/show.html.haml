= image_tag @post.image.url(:medium)
%h1 Show ME
%h2=@post.title
%h2=@post.description
%h2=@post.user.name

=link_to "Edit", edit_post_path(@post)
=link_to "Home", root_path
=link_to "Destroy", post_path(@post), method: :delete, data: {confirm: "Do you want to destroy"}